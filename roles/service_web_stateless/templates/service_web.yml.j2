---
apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}
  namespace: {{ service_namespace }}
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: {{ app_name }}